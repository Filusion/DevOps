# DevOps

This repository contains different deployment methods for the same application:

- **Vagrant + Cloud-Init**: Provision and deploy the app using a virtual machine with Vagrant and automated configuration via Cloud-Init.  
- **Docker Compose**: Deploy the app using Docker Compose for containerized environments.  

Use these examples as references for automated and containerized deployments.